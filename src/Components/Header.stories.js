import Header from "./Header";
import React, { useState } from "react";

export default {
    title: "Test/Header",
    component: Header,
};


export const Header = {
    args: {
        label: "Header"
    }
}